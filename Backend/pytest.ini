[pytest]
# Inclui raiz do backend para imports absolutos em `App.*`.
pythonpath = .
# Pasta oficial da suite de testes.
testpaths = tests
# Executa em modo quiet para output mais limpo no terminal.
addopts = -q
